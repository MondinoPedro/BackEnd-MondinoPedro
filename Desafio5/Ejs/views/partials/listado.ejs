<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Form Ejs</title>
    <style>
        .input-container{
            width: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .input-container input{
            height: 40px;
            width: 300px;
            text-align: center;
            margin: 5px;
        }

        h1{
            text-align: center;
        }

        h3{
            text-align: center;
        }

        li{
            list-style: none;
            margin: 10px;
            text-align: center;
        }
        
    </style>
</head>
<body>

<h1>Listado de productos: </h1>
    <%if(productos.length > 0){%>
        <ul>
            <%productos.forEach( produ =>{%>
                <li><b>Nombre:</b> <%= produ.nombre%> - <b>Precio:</b> $<%= produ.precio%> - <b>Descricpcion:</b> <%= produ.descripcion%></li>
            <%})%>
        </ul>
    <%}else{%>
        <h3>No hay productos en la lista!</h3>
    <%}%>
<form  class="input-container" method="POST">
    <input type="submit" value="Agregar producto"/>
</form>
      
</body>
</html>  
        